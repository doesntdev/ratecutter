import { useState } from 'react'; import QuickInput from './components/QuickInput'; import ResultsDisplay from './components/ResultsDisplay'; import LeadCapture from './components/LeadCapture'; import { runCalculations } from './utils/calculations'; function App() { const [step, setStep] = useState('input'); const [calculationData, setCalculationData] = useState(null); const [results, setResults] = useState(null); const handleInputSubmit = (data) => { setCalculationData(data); const calcResults = runCalculations(data); setResults(calcResults); setStep('results'); }; const handleRestart = () => { setStep('input'); setCalculationData(null); setResults(null); }; const handleSave = () => { setStep('capture'); }; const handleSaved = () => { setStep('input'); setCalculationData(null); setResults(null); }; return ( <div className="min-h-screen bg-white"> {/* Header - Forward Payments style */} <header className="bg-white border-b border-gray-100"> <div className="max-w-6xl mx-auto px-4 py-4 flex items-center justify-between"> <div className="flex items-center space-x-3"> {/* Green chevron logo like Forward Payments */} <div className="flex flex-col space-y-[-4px]"> <svg width="32" height="14" viewBox="0 0 32 14" fill="none" xmlns="http://www.w3.org/2000/svg"> <path d="M0 0L16 14L32 0H24L16 7L8 0H0Z" fill="#6FCA3A"/> </svg> <svg width="32" height="14" viewBox="0 0 32 14" fill="none" xmlns="http://www.w3.org/2000/svg"> <path d="M0 0L16 14L32 0H24L16 7L8 0H0Z" fill="#6FCA3A"/> </svg> </div> <span className="text-2xl font-bold text-gray-900">RateCutter</span> </div> <nav className="hidden sm:flex space-x-8"> <a href="#" className="text-gray-600 hover:text-gray-900 font-medium">How It Works</a> <a href="#" className="text-gray-600 hover:text-gray-900 font-medium">About</a> </nav> <button className="hidden sm:block bg-brand-primary hover:bg-brand-dark text-white font-semibold px-6 py-2.5 rounded-lg transition-colors"> Get Started </button> </div> </header> {/* Main Content */} <main className="py-12 px-4"> <div className="max-w-4xl mx-auto"> {step === 'input' && ( <div className="text-center mb-10"> <h1 className="text-4xl sm:text-5xl font-bold text-gray-900 mb-4"> See how much you could save. </h1> <p className="text-xl text-gray-600"> Powerful rate comparison for modern businesses. </p> </div> )} {step === 'input' && <QuickInput onSubmit={handleInputSubmit} />} {step === 'results' && results && <ResultsDisplay results={results} onRestart={handleRestart} onSave={handleSave} />} {step === 'capture' && <LeadCapture calculationData={calculationData} onSaved={handleSaved} />} </div> </main> {/* Footer */} <footer className="mt-auto py-6 border-t border-gray-100"> <div className="max-w-4xl mx-auto px-4 text-center"> <p className="text-gray-500 text-sm"> RateCutter - Helping merchants find better processing rates </p> <p className="text-gray-400 text-xs mt-2"> © 2026 RateCutter. All calculations are estimates. </p> </div> </footer> </div> ); } export default App;